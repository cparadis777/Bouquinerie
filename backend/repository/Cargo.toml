[package]
name = "repository"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
uuid = "1.23.2"
domain = {path = "../domain"}
sea-orm = { version = "2.0.0-rc.38", features = ["macros"] }
chrono = { version = "0.4", features = ["serde"] }

[features]
mock = []
