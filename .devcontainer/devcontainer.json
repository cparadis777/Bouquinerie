{
  "name": "Bouquinerie",
  "dockerComposeFile": "docker-compose.yml",
  "service": "devcontainer",
  "workspaceFolder": "/workspace",
  "remoteUser": "root",
  "containerUser": "root",
  "features": {},

  "postCreateCommand": "cargo install sea-orm-cli && cargo install cargo-watch && prek install",
  "forwardPorts": [3000, "postgres:5432"],
  "portsAttributes": {
    "3000": { "label": "Rust API", "protocol": "http" },
    "5432": {"label": "Postgres"}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "Vue.volar",
        "tamasfe.even-better-toml",
        "editorconfig.editorconfig",
        "task.vscode-task",
        "bierner.github-markdown-preview"
      ]
    }
  },

  "mounts": [
    "source=~/.ssh/,target=/root/.ssh,type=bind,z,consistency=cached",
    "source=${env:SSH_AUTH_SOCK},target=/tmp/ssh-agent.sock,type=bind,z"
  ],

  "containerEnv": {
    "SSH_AUTH_SOCK": "/tmp/ssh-agent.sock"
  }
}
